<%= form_for(@post) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :content, placeholder: "Ask me anything..." %>
	</div>
	<%= f.submit "Submit", class: "btn btn-large btn-primary" %>
<% end %>